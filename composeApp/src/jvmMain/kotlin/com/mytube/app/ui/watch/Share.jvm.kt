package com.mytube.app.ui.watch

import androidx.compose.runtime.Composable

/** Nothing to share from: the JVM target exists to run tests. */
@Composable
actual fun rememberShare(): (String) -> Unit = {}
